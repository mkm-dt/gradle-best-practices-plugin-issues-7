plugins {
    id 'java-gradle-plugin'
    id 'groovy'
    id("com.autonomousapps.plugin-best-practices-plugin") version "0.9"
}

//repositories {
//    mavenCentral()
//}

gradlePlugin {
    // Define the plugin
    plugins {
        greeting {
            id = 'best.practices.groovy.greeting'
            implementationClass = 'best.practices.groovy.BestPracticesGroovyPlugin'
        }
    }
}
